import { FiltercompledPipe } from './filtercompled.pipe';

describe('FiltercompledPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercompledPipe();
    expect(pipe).toBeTruthy();
  });
});
